# Shared Types

TypeScript types and interfaces shared across the application.

## Guidelines

- Use clear, descriptive names
- Document complex types with comments
- Prefer interfaces for object shapes
- Use types for unions and intersections
