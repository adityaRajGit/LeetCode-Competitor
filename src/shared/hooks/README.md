# Shared Hooks

Custom React hooks for reusable logic.

## Naming Convention

All hooks should start with `use` prefix (e.g., `useLocalStorage`, `useDebounce`)

## Example Structure

```typescript
export function useExample() {
  // Hook implementation
  return { /* ... */ }
}
```
